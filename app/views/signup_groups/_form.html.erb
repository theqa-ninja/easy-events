<%= form_with(model: signup_group) do |form| %>
  <% if signup_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(signup_group.errors.count, "error") %> prohibited this signup_group from being saved:</h2>

      <ul>
        <% signup_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :group_name, style: "display: block" %>
    <%= form.text_field :group_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
