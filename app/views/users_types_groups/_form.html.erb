<%= form_with(model: users_types_group) do |form| %>
  <% if users_types_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(users_types_group.errors.count, "error") %> prohibited this users_types_group from being saved:</h2>

      <ul>
        <% users_types_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :user_type_id, style: "display: block" %>
    <%= form.number_field :user_type_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.number_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
