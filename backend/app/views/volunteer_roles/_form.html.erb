<%= form_with(model: volunteer_role) do |form| %>
  <% if volunteer_role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(volunteer_role.errors.count, "error") %> prohibited this volunteer_role from being saved:</h2>

      <ul>
        <% volunteer_role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
