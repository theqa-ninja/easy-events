<%= link_to raw("&laquo; Back to events"), events_path %>
<%= render @event %>
<% if @user_is_event_coordinator_or_admin %>
<div>
  <h3>Event Coordinator Actions</h3>
  <p><%= link_to "Edit this event", edit_api_v1_event_path(@event) %></p>
  <p><%= button_to "Destroy this event", @event, method: :delete %></p>
</div>
<% end %>
<%= link_to raw("&laquo; Back to events"), events_path %>